# Waterstone Companies — static site

Static HTML + `styles.css` + `script.js` (same pattern as the Denver Doors project).

## Local preview

Open `index.html` in a browser, or serve the folder:

```bash
npx serve .
```

## Client / dev checklist

- [ ] Replace placeholder phone `(555) 123-4567` and `hello@waterstonehomes.com` sitewide
- [ ] Set real domain in `canonical` URLs, `sitemap.xml`, and `robots.txt`
- [ ] Add `assets/logo.png` and optional `assets/hero-bg.jpg` from Google Drive
- [ ] Drop project photos into `assets/images/<service-slug>/`
- [ ] Wire `contact/index.html` form to Formspree, Netlify Forms, or CRM
- [ ] Embed chat widget snippet before `</body>` on all pages (optional)
- [ ] Add GA4 or analytics when ID is available
- [ ] Expand blog stubs under `blog/*/index.html` to full 800–1500 word posts
- [ ] Privacy & Terms: replace `#` footer links with real pages or Termly/Iubenda

## Project docs

See `project-tasklist.md`, `project-style-guide.md`, and `project-site-map.md` in this folder.
